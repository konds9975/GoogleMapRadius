# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GoogleMapRadius' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

	pod 'GooglePlaces'
	pod 'GoogleMaps'

  # Pods for GoogleMapRadius

  target 'GoogleMapRadiusTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GoogleMapRadiusUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
